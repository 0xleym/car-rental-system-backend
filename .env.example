# Server
PORT=5001

# Database
DB_USER="postgres"
DB_HOST="localhost"
DB_DATABASE="cms"
DB_PORT=5432
DB_PASSWORD="your_db_password_here"

# CORS (use specific origin in production, e.g., https://yourfrontend.com)
CORS_ORIGIN=*

# JWT (use a strong, random secret in production — at least 32 characters)
JWT_SECRET="change-me-to-a-strong-random-secret-at-least-32-chars"
JWT_EXPIRES_IN="7d"

# Bcrypt
BCRYPT_SALT_ROUNDS=12
